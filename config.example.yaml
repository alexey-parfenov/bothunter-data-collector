# Example configuration for BotHunter Data Collector.
# This file contains no credentials or production settings.

exchanges:
  - exchange_a
  - exchange_b
  - exchange_c

health:
  stale_after_ms: 5000
  max_event_latency_ms: 3000

storage:
  output_dir: data

logging:
  level: INFO
